interface:
  display_name: "Kalimba ToneTrace Compiler"
  short_description: "Convert jianpu photos into melody, chord, accompaniment, and rhythm-aware song JSON."
  default_prompt: "Use $kalimba-tone-trace-compiler to convert numbered-notation sheet photos into validated Kalimba ToneTrace song JSON."

policy:
  allow_implicit_invocation: true
